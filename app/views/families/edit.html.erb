<%- model_class = Family -%>
<div class="page-header">
  <h1><%=t '.title', :default => [:'helpers.titles.edit', 'Edit %{model}'], :model => model_class.model_name.human.titleize %></h1>
</div>
<%= render :partial => 'form' %>

<%= render partial: 'shared/parents_table', locals: {parents: @family.parents, skip: {family: true}} %>
<%= render partial: 'shared/children_table', locals: {children: @family.children, skip: {family: true}} %>
